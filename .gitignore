# Jekyll build output — never commit these
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata

# Gemfile.lock should be committed (pins gem versions for reproducible CI builds)
# So it is intentionally NOT listed here

# Dependencies installed locally
vendor/

# macOS
.DS_Store
.DS_Store?
._*

# Editor
*.swp
*.swo
.idea/
.vscode/
